<%= form_for(products_promotion) do |f| %>
  <% if products_promotion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(products_promotion.errors.count, "error") %> prohibited this products_promotion from being saved:</h2>

      <ul>
      <% products_promotion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :promotion_id %>
    <%= f.text_field :promotion_id %>
  </div>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.text_field :product_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
